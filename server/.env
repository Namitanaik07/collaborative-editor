# server/.env

# Add the database name 'collaborativeEditorDB' after the cluster hostname:
MONGO_URI="mongodb+srv://namitaanaik07_db_user:Jaishreekrishna07@collaborativecodeeditor.u5ytt6h.mongodb.net/?appName=CollaborativeCodeEditor" 

PORT=5000
# [nodemon] starting `node server.js`
# Server listening on port 5000 therefore successfully added the database name in the .env file.
# Server listening on port 5000
